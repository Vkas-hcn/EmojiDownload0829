<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Cache directory for sharing images -->
    <cache-path
        name="cache"
        path="." />

    <!-- External files directory for downloads -->
    <external-files-path
        name="external_files"
        path="." />

    <!-- External storage for downloads (legacy) -->
    <external-path
        name="external"
        path="." />
</paths>